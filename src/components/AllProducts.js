import React from 'react'
import { useStaticQuery, graphql } from 'gatsby'

const AllProducts = () => {
  return (
    <div>AllProducts</div>
  )
}

const query = graphql`

`
export default AllProducts